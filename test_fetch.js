import * as dotenv from 'dotenv';
dotenv.config({ path: 'apps/kongila-web/.env.local' });
// Vercel deployment URL
const url = 'https://kongila-admin.vercel.app/api/db'; // wait, I don't know the URL!
